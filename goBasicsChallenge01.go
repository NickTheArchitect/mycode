// Programs start running in package "main"
package main

// This program uses the packages with import paths "fmt" and "math/rand"
import (
    "fmt"
)

// Creating a function main()
func main() {
    var  city, state = "Wayzata", "Minnesota"
    fmt.Println("My name is Nick.")
    fmt.Println("I live in the unpronouncable city of", city, ", which is in the great northern state of", state, ".")
}

